syntax="proto3";

package sortingService;

service SortingService {
    rpc Sort(SortRequest) returns (Response) {}
}

message SortRequest {
    bool sorted = 1;
    repeated int32 tableToSort = 2;
}

message Response {
    bool sorted = 1;
    repeated int32 sortedTable = 2;
}